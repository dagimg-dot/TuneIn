import { Light } from "../../../styles/colors";

function Logo() {
  return (
    <div
      css={{
        fontFamily: "Z003",
        padding: "4px 8px",
        color: Light.textColor,
        fontSize: "32px",
      }}
    >
      Zmarie
    </div>
  );
}

export default Logo;
